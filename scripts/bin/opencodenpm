#!/usr/bin/env node
/**
 * OpenCode 中文汉化工具 - 统一入口
 *
 * 用法:
 *   opencodenpm update    - 更新源码
 *   opencodenpm apply     - 应用汉化 (--backup --report --strict --dry-run)
 *   opencodenpm build     - 编译构建
 *   opencodenpm verify    - 验证配置 (--detailed --dry-run)
 *   opencodenpm full      - 完整工作流
 *   opencodenpm rollback  - 回滚备份 (--list --id <备份ID>)
 *   opencodenpm env       - 检查环境
 *   opencodenpm config    - 显示配置
 *   opencodenpm launch    - 启动 OpenCode
 *   opencodenpm deploy    - 部署到全局
 *   opencodenpm helper    - 智谱助手
 *   opencodenpm package   - 打包发布
 */

require('../core/cli.js').run();
