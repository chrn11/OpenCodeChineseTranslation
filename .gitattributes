# 强制 shell 脚本使用 LF 换行符
*.sh text eol=lf
*.bash text eol=lf
*.zsh text eol=lf

# PowerShell 脚本使用 CRLF (Windows 标准)
*.ps1 text eol=crlf
*.psm1 text eol=crlf

# 其他文本文件
*.md text eol=lf
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# 从上游 sync 时保留本地版本（不被上游覆盖）
# 这些目录/文件使用了不同于上游的结构
opencode-i18n/** merge=ours
scripts/core/i18n.js merge=ours
scripts/core/translator.js merge=ours
